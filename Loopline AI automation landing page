<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Loopline — AI automation for lean businesses</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

:root{
  --bg: #0B0E1A;
  --bg-alt: #12172B;
  --panel: #161C33;
  --line: #262E4D;
  --text: #EEF0EA;
  --text-dim: #8A91AD;
  --accent: #00E5A0;
  --accent-dim: #0A5C48;
  --warm: #FFB454;
  --radius: 10px;
}

*{ box-sizing: border-box; margin:0; padding:0; }

html{ scroll-behavior: smooth; }

body{
  background: var(--bg);
  color: var(--text);
  font-family: 'Inter', sans-serif;
  line-height: 1.55;
  overflow-x: hidden;
}

.mono{ font-family: 'JetBrains Mono', monospace; }

h1,h2,h3{
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 600;
  letter-spacing: -0.02em;
  line-height: 1.08;
}

a{ color: inherit; text-decoration: none; }

button{ font-family: inherit; cursor: pointer; }

:focus-visible{
  outline: 2px solid var(--accent);
  outline-offset: 3px;
}

.wrap{
  max-width: 1120px;
  margin: 0 auto;
  padding: 0 28px;
}

/* background grid texture */
body::before{
  content:"";
  position: fixed;
  inset: 0;
  background-image:
    linear-gradient(var(--line) 1px, transparent 1px),
    linear-gradient(90deg, var(--line) 1px, transparent 1px);
  background-size: 64px 64px;
  opacity: 0.18;
  pointer-events: none;
  z-index: 0;
  mask-image: radial-gradient(ellipse 80% 60% at 50% 0%, black 40%, transparent 90%);
}

/* NAV */
nav{
  position: sticky;
  top: 0;
  z-index: 50;
  backdrop-filter: blur(10px);
  background: rgba(11,14,26,0.75);
  border-bottom: 1px solid var(--line);
}
nav .wrap{
  display:flex;
  align-items:center;
  justify-content: space-between;
  height: 68px;
}
.logo{
  font-family:'Space Grotesk', sans-serif;
  font-weight:700;
  font-size: 1.15rem;
  display:flex;
  align-items:center;
  gap:8px;
}
.logo .dot{
  width:8px; height:8px; border-radius:50%;
  background: var(--accent);
  box-shadow: 0 0 12px var(--accent);
}
.nav-links{ display:flex; align-items:center; gap:28px; }
.nav-links a{
  font-size: 0.92rem;
  color: var(--text-dim);
  transition: color 0.2s;
}
.nav-links a:hover{ color: var(--text); }
.nav-cta{
  background: var(--accent);
  color: #06251C;
  padding: 9px 18px;
  border-radius: 100px;
  font-weight: 600;
  font-size: 0.88rem;
  border: none;
  transition: transform 0.15s, box-shadow 0.15s;
}
.nav-cta:hover{ transform: translateY(-1px); box-shadow: 0 6px 20px rgba(0,229,160,0.25); }

/* HERO */
.hero{
  position: relative;
  z-index: 1;
  padding: 96px 0 60px;
  display:grid;
  grid-template-columns: 1.05fr 0.95fr;
  gap: 40px;
  align-items:center;
}
.eyebrow{
  font-family:'JetBrains Mono', monospace;
  font-size: 0.78rem;
  letter-spacing: 0.12em;
  color: var(--accent);
  text-transform: uppercase;
  display:flex;
  align-items:center;
  gap:10px;
  margin-bottom: 20px;
}
.eyebrow::before{
  content:"";
  width: 22px; height:1px;
  background: var(--accent);
}
.hero h1{
  font-size: clamp(2.2rem, 4.4vw, 3.4rem);
  margin-bottom: 22px;
}
.hero h1 em{
  font-style: normal;
  color: var(--accent);
}
.hero p.sub{
  font-size: 1.08rem;
  color: var(--text-dim);
  max-width: 480px;
  margin-bottom: 34px;
}

.waitlist-form{
  display:flex;
  gap:10px;
  max-width: 460px;
  flex-wrap: wrap;
}
.waitlist-form input[type=email]{
  flex: 1 1 240px;
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  padding: 13px 16px;
  color: var(--text);
  font-size: 0.95rem;
  font-family: 'Inter', sans-serif;
}
.waitlist-form input[type=email]::placeholder{ color: var(--text-dim); }
.waitlist-form input[type=email]:focus{ border-color: var(--accent); }
.waitlist-form button{
  background: var(--accent);
  color: #06251C;
  border: none;
  padding: 13px 22px;
  border-radius: var(--radius);
  font-weight: 600;
  font-size: 0.95rem;
  white-space: nowrap;
  transition: transform 0.15s, box-shadow 0.15s;
}
.waitlist-form button:hover{ transform: translateY(-1px); box-shadow: 0 6px 20px rgba(0,229,160,0.25); }
.waitlist-form button:disabled{ opacity: 0.6; cursor: default; transform:none; box-shadow:none; }

.hero-note{
  margin-top: 14px;
  font-size: 0.82rem;
  color: var(--text-dim);
  font-family: 'JetBrains Mono', monospace;
}
.hero-note strong{ color: var(--accent); }

.form-msg{
  margin-top: 12px;
  font-size: 0.88rem;
  min-height: 1.2em;
}
.form-msg.ok{ color: var(--accent); }
.form-msg.err{ color: var(--warm); }

/* HERO VISUAL - node graph */
.hero-visual{
  position: relative;
  aspect-ratio: 1 / 1;
  max-width: 480px;
  justify-self: end;
}
.hero-visual svg{ width: 100%; height: 100%; overflow: visible; }

.node-path{
  fill: none;
  stroke: var(--line);
  stroke-width: 1.5;
}
.node-path.active{
  stroke: var(--accent-dim);
}
.node-circle{
  fill: var(--panel);
  stroke: var(--line);
  stroke-width: 1.5;
}
.node-circle.chaos{ stroke: #3A2E4D; fill: #1A1730; }
.node-circle.hub{
  fill: var(--accent);
  stroke: var(--accent);
}
.node-label{
  font-family: 'JetBrains Mono', monospace;
  font-size: 9px;
  fill: var(--text-dim);
}
.pulse{
  fill: var(--accent);
  filter: drop-shadow(0 0 6px var(--accent));
}

@media (prefers-reduced-motion: no-preference){
  .draw{
    stroke-dasharray: 300;
    stroke-dashoffset: 300;
    animation: draw 1.6s ease forwards;
  }
  .pulse{ offset-rotate: 0deg; animation: travel 3.4s linear infinite; }
  .pulse.p2{ animation-delay: 1.1s; }
  .pulse.p3{ animation-delay: 2.2s; }
  .hub{ animation: beat 2.4s ease-in-out infinite; }
}
@keyframes draw{ to{ stroke-dashoffset: 0; } }
@keyframes travel{ from{ offset-distance: 0%; } to{ offset-distance: 100%; } }
@keyframes beat{ 0%,100%{ transform: scale(1); } 50%{ transform: scale(1.12); } }

/* SECTIONS */
section{
  position: relative;
  z-index:1;
  padding: 90px 0;
  border-top: 1px solid var(--line);
}
.section-head{
  max-width: 620px;
  margin-bottom: 52px;
}
.section-head .eyebrow{ margin-bottom: 14px; }
.section-head h2{ font-size: clamp(1.6rem, 3vw, 2.2rem); margin-bottom: 14px; }
.section-head p{ color: var(--text-dim); font-size: 1rem; }

/* PROBLEM - chaos list */
.chaos-grid{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}
.chaos-card{
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  padding: 22px;
}
.chaos-card .tag{
  font-family:'JetBrains Mono', monospace;
  font-size: 0.72rem;
  color: var(--warm);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: 10px;
  display:block;
}
.chaos-card p{ font-size: 0.94rem; color: var(--text); }

/* HOW IT WORKS - real sequence, numbers justified */
.steps{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0;
  position: relative;
}
.step{
  padding: 0 24px 0 0;
  position: relative;
}
.step:not(:last-child)::after{
  content:"";
  position:absolute;
  top: 20px;
  right: -8px;
  width: 16px; height:1px;
  background: var(--line);
  display: none;
}
.step .num{
  font-family:'JetBrains Mono', monospace;
  color: var(--accent);
  font-size: 0.85rem;
  margin-bottom: 14px;
  display:block;
}
.step h3{ font-size: 1.2rem; margin-bottom: 10px; }
.step p{ color: var(--text-dim); font-size: 0.94rem; }

/* WHAT WE AUTOMATE */
.cards{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}
.card{
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  padding: 26px;
  transition: border-color 0.2s, transform 0.2s;
}
.card:hover{ border-color: var(--accent-dim); transform: translateY(-2px); }
.card h3{ font-size: 1.05rem; margin-bottom: 8px; }
.card p{ font-size: 0.9rem; color: var(--text-dim); }
.card .icon{
  width: 34px; height:34px;
  border-radius: 8px;
  background: rgba(0,229,160,0.1);
  border: 1px solid var(--accent-dim);
  display:flex; align-items:center; justify-content:center;
  margin-bottom: 16px;
  color: var(--accent);
  font-family:'JetBrains Mono', monospace;
  font-size: 0.9rem;
}

/* WAITLIST SECTION */
.waitlist-section{
  text-align:center;
}
.waitlist-section .section-head{
  margin: 0 auto 40px;
  text-align:center;
}
.waitlist-section .eyebrow{ justify-content:center; }
.waitlist-section .waitlist-form{
  margin: 0 auto;
}
.count-badge{
  display:inline-flex;
  align-items:center;
  gap:8px;
  margin-top: 26px;
  padding: 8px 16px;
  border-radius: 100px;
  border: 1px solid var(--line);
  background: var(--panel);
  font-family: 'JetBrains Mono', monospace;
  font-size: 0.82rem;
  color: var(--text-dim);
}
.count-badge .n{ color: var(--accent); font-weight: 600; }
.count-badge .live-dot{
  width:6px; height:6px; border-radius:50%;
  background: var(--accent);
  box-shadow: 0 0 8px var(--accent);
}

/* FOOTER */
footer{
  padding: 40px 0;
  border-top: 1px solid var(--line);
  display:flex;
  justify-content: space-between;
  align-items:center;
  color: var(--text-dim);
  font-size: 0.85rem;
  position: relative;
  z-index:1;
}

@media (max-width: 860px){
  .hero{ grid-template-columns: 1fr; padding-top: 48px; }
  .hero-visual{ max-width: 320px; justify-self: center; order:-1; margin-bottom: 12px; }
  .chaos-grid, .cards{ grid-template-columns: 1fr 1fr; }
  .steps{ grid-template-columns: 1fr; gap: 28px; }
  .nav-links{ display:none; }
  footer{ flex-direction: column; gap: 10px; text-align:center; }
}
@media (max-width: 520px){
  .chaos-grid, .cards{ grid-template-columns: 1fr; }
}
</style>
</head>
<body>

<nav>
  <div class="wrap">
    <div class="logo"><span class="dot"></span>Loopline</div>
    <div class="nav-links">
      <a href="#problem">The problem</a>
      <a href="#how">How it works</a>
      <a href="#automate">What we automate</a>
    </div>
    <button class="nav-cta" onclick="document.getElementById('join').scrollIntoView({behavior:'smooth'})">Join waitlist</button>
  </div>
</nav>

<section class="hero wrap" style="border-top:none;">
  <div>
    <div class="eyebrow">AI automation for lean businesses</div>
    <h1>Your business runs on tasks that don't need <em>you</em> anymore.</h1>
    <p class="sub">Loopline builds the automations that handle your inbox, your follow-ups, and your admin — so you stop doing $15/hour work at midnight.</p>

    <form class="waitlist-form" id="waitlistForm">
      <input type="email" id="emailInput" placeholder="you@yourbusiness.com" required autocomplete="email">
      <button type="submit" id="submitBtn">Join the waitlist</button>
    </form>
    <div class="form-msg" id="formMsg"></div>
    <div class="hero-note">Early members get <strong>free setup</strong> on their first automation.</div>
  </div>

  <div class="hero-visual">
    <svg viewBox="0 0 400 400">
      <!-- scattered chaos nodes left -->
      <circle class="node-circle chaos" cx="40" cy="60" r="14"/>
      <text class="node-label" x="40" y="63" text-anchor="middle">mail</text>

      <circle class="node-circle chaos" cx="30" cy="160" r="14"/>
      <text class="node-label" x="30" y="163" text-anchor="middle">DMs</text>

      <circle class="node-circle chaos" cx="55" cy="260" r="14"/>
      <text class="node-label" x="55" y="263" text-anchor="middle">forms</text>

      <circle class="node-circle chaos" cx="35" cy="340" r="14"/>
      <text class="node-label" x="35" y="343" text-anchor="middle">CRM</text>

      <!-- paths converging into hub -->
      <path class="node-path draw" d="M54,60 C140,60 150,200 195,200"/>
      <path class="node-path draw" d="M44,160 C140,160 150,200 195,200"/>
      <path class="node-path draw" d="M69,260 C140,260 150,200 195,200"/>
      <path class="node-path draw" d="M49,340 C140,340 150,200 195,200"/>

      <!-- hub -->
      <circle class="node-circle hub" cx="200" cy="200" r="20"/>
      <text class="node-label" x="200" y="204" text-anchor="middle" fill="#06251C" font-weight="600">AI</text>

      <!-- paths out to organized outputs -->
      <path id="outpath1" class="node-path draw active" d="M220,200 C280,200 290,90 350,90"/>
      <path id="outpath2" class="node-path draw active" d="M220,200 C290,200 300,200 360,200"/>
      <path id="outpath3" class="node-path draw active" d="M220,200 C280,200 290,310 350,310"/>

      <circle class="node-circle" cx="360" cy="90" r="13"/>
      <text class="node-label" x="360" y="93" text-anchor="middle">sent</text>
      <circle class="node-circle" cx="368" cy="200" r="13"/>
      <text class="node-label" x="368" y="203" text-anchor="middle">booked</text>
      <circle class="node-circle" cx="360" cy="310" r="13"/>
      <text class="node-label" x="360" y="313" text-anchor="middle">logged</text>

      <!-- traveling pulses -->
      <circle class="pulse p1" r="3" style="offset-path: path('M220,200 C280,200 290,90 350,90');"/>
      <circle class="pulse p2" r="3" style="offset-path: path('M220,200 C290,200 300,200 360,200');"/>
      <circle class="pulse p3" r="3" style="offset-path: path('M220,200 C280,200 290,310 350,310');"/>
    </svg>
  </div>
</section>

<section id="problem">
  <div class="wrap">
    <div class="section-head">
      <div class="eyebrow">The problem</div>
      <h2>You didn't start a business to become its inbox.</h2>
      <p>Every hour spent on repeat tasks is an hour not spent on clients, sales, or the work only you can do.</p>
    </div>
    <div class="chaos-grid">
      <div class="chaos-card"><span class="tag">Every morning</span><p>Sorting the same 40 emails into "urgent," "later," and "why is this still here."</p></div>
      <div class="chaos-card"><span class="tag">Every lead</span><p>Manually copying a form submission into a spreadsheet, then into a CRM, then forgetting to follow up.</p></div>
      <div class="chaos-card"><span class="tag">Every booking</span><p>Back-and-forth scheduling messages that could've been one link.</p></div>
      <div class="chaos-card"><span class="tag">Every new client</span><p>Re-typing the same welcome info, contract, and onboarding steps by hand.</p></div>
      <div class="chaos-card"><span class="tag">Every month</span><p>Pulling numbers from five places to figure out how the business actually did.</p></div>
      <div class="chaos-card"><span class="tag">Every "quick task"</span><p>The 10-minute jobs that happen 20 times a week and quietly eat your day.</p></div>
    </div>
  </div>
</section>

<section id="how">
  <div class="wrap">
    <div class="section-head">
      <div class="eyebrow">How it works</div>
      <h2>Three steps, then it runs itself.</h2>
      <p>No new software to learn. We build inside the tools you already use.</p>
    </div>
    <div class="steps">
      <div class="step">
        <span class="num">01 — Connect</span>
        <h3>We map your busywork</h3>
        <p>A short call to see where your time actually goes — inbox, calendar, leads, client admin.</p>
      </div>
      <div class="step">
        <span class="num">02 — Configure</span>
        <h3>We build the automation</h3>
        <p>Loopline sets up the workflow inside your existing tools. You review it before it goes live.</p>
      </div>
      <div class="step">
        <span class="num">03 — Coast</span>
        <h3>It runs without you</h3>
        <p>The task happens automatically, every time — and you get a plain-language summary, not a dashboard to babysit.</p>
      </div>
    </div>
  </div>
</section>

<section id="automate">
  <div class="wrap">
    <div class="section-head">
      <div class="eyebrow">What we automate</div>
      <h2>Built for coaches, agents, and solo operators.</h2>
      <p>If it's repetitive and rule-based, it's a candidate for automation.</p>
    </div>
    <div class="cards">
      <div class="card"><div class="icon">✉</div><h3>Inbox triage</h3><p>Priority emails surfaced, routine ones answered or filed automatically.</p></div>
      <div class="card"><div class="icon">→</div><h3>Lead follow-up</h3><p>New leads get an instant, personal-sounding reply — no more cold trails.</p></div>
      <div class="card"><div class="icon">◷</div><h3>Appointment booking</h3><p>Self-serve scheduling with automatic reminders and reschedules.</p></div>
      <div class="card"><div class="icon">◧</div><h3>Client onboarding</h3><p>Welcome emails, contracts, and intake forms sent the moment someone signs.</p></div>
      <div class="card"><div class="icon">≡</div><h3>Reporting</h3><p>A weekly summary of what happened in your business, written in plain English.</p></div>
      <div class="card"><div class="icon">⟳</div><h3>Custom workflows</h3><p>Anything specific to how you work — if it's routine, we can likely automate it.</p></div>
    </div>
  </div>
</section>

<section class="waitlist-section" id="join">
  <div class="wrap">
    <div class="section-head">
      <div class="eyebrow">Early access</div>
      <h2>Be first when Loopline opens.</h2>
      <p>Early members get free setup on their first automation, before we open to everyone else.</p>
    </div>
    <form class="waitlist-form" id="waitlistForm2">
      <input type="email" id="emailInput2" placeholder="you@yourbusiness.com" required autocomplete="email">
      <button type="submit" id="submitBtn2">Join the waitlist</button>
    </form>
    <div class="form-msg" id="formMsg2"></div>
    <div class="count-badge"><span class="live-dot"></span><span class="n" id="waitlistCount">—</span>&nbsp;people already on the list</div>
  </div>
</section>

<footer class="wrap">
  <div><span style="color:var(--text)">Loopline</span> — AI automation for lean businesses</div>
  <div>Non-voice, remote-friendly, no code required.</div>
</footer>

<script>
async function getCount(){
  try{
    const res = await window.storage.list('waitlist:', true);
    return res && res.keys ? res.keys.length : 0;
  }catch(e){ return 0; }
}

async function refreshCount(){
  const el = document.getElementById('waitlistCount');
  const n = await getCount();
  el.textContent = n;
}

async function joinWaitlist(email, msgEl, btnEl){
  email = email.trim().toLowerCase();
  if(!email || !email.includes('@')){
    msgEl.textContent = "That doesn't look like a valid email.";
    msgEl.className = 'form-msg err';
    return;
  }
  btnEl.disabled = true;
  const originalText = btnEl.textContent;
  btnEl.textContent = 'Joining…';
  try{
    const key = 'waitlist:' + email;
    let existing = null;
    try{ existing = await window.storage.get(key, true); }catch(e){ existing = null; }
    if(existing){
      msgEl.textContent = "You're already on the list!";
      msgEl.className = 'form-msg ok';
    } else {
      await window.storage.set(key, JSON.stringify({ email, joinedAt: new Date().toISOString() }), true);
      msgEl.textContent = "You're in. We'll email you when Loopline opens.";
      msgEl.className = 'form-msg ok';
    }
    await refreshCount();
  }catch(e){
    msgEl.textContent = 'Something went wrong — please try again.';
    msgEl.className = 'form-msg err';
  }finally{
    btnEl.disabled = false;
    btnEl.textContent = originalText;
  }
}

document.getElementById('waitlistForm').addEventListener('submit', function(e){
  e.preventDefault();
  const email = document.getElementById('emailInput').value;
  joinWaitlist(email, document.getElementById('formMsg'), document.getElementById('submitBtn'));
});

document.getElementById('waitlistForm2').addEventListener('submit', function(e){
  e.preventDefault();
  const email = document.getElementById('emailInput2').value;
  joinWaitlist(email, document.getElementById('formMsg2'), document.getElementById('submitBtn2'));
});

refreshCount();
</script>

</body>
</html>
